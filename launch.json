{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug generate-express",
      "program": "${workspaceFolder}/node_modules/.bin/generate-express",
      "cwd": "${workspaceFolder}",
      "args": [],
      "console": "integratedTerminal",
      "internalConsoleOptions": "neverOpen"
    }
  ]
}